type Mutation {
  login(
    email: String!
    password: String!
  ): JWTResponse
}

type Mutation {
  refresh(
    refresh_token: String!
  ): JWTResponse
}

type JWTResponse {
  token: String!
  refresh_token: String!
}

